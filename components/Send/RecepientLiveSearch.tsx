import { View } from "react-native";

interface RecepientLiveSearchProps {
    uid: string;
}
export default function RecepientLiveSearch({ uid }: RecepientLiveSearchProps) {
    return <View></View>;
}
